import Foundation

struct HotPepperResponse: Codable {
    let results: Results
}

struct Results: Codable {
    let shop: [Restaurant]
}

struct Restaurant: Codable {
    let id: String
    let name: String
    let address: String
}
